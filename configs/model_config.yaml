model:
  name: random_forest   # options: linear_regression, random_forest

linear_regression:
  # no hyperparameters needed for a plain baseline

random_forest:
  n_estimators: 200
  max_depth: 15
  min_samples_leaf: 5
  random_state: 42
  n_jobs: -1